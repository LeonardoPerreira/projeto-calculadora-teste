const soma = (a,b) => {
    res = a + b;
    return(res) 
}

const subtrcao = (a,b) => {
    res = a - b;
    return(res)
}

const multiplicacao = (a,b) => {
    res = a * b;
    return(res);
}

const divisao = () => {
    if(b != 0){
    res = a / b;
        return(res)
    } else {
        console.error("O denominador não pode ser 0 vagabundo")
    }
}